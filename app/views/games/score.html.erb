<h1>Result</h1>

<% if @word.empty? %>
  <p>You must guess a word...</p>
<% elsif @english_word && @included %>
  <p><strong>Congratulations!</strong> <%= @word %> is a valid English word! Your score is <%= @compute_score %></p>
<% elsif @english_word %>
 <p>Sorry but <strong><%= @word %></strong> can't be built out of <%= @letters.join(", ") %></p>
<% else %>
  <p>Sorry but <strong><%= @word %></strong> does not seem to be a valid English word...</p>
<% end %>

<%= link_to "Play again", new_path %>
